# Postgres connection string
DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"

# NextAuth / Auth.js secret — generate with `openssl rand -base64 32`
AUTH_SECRET="replace-with-a-random-secret"

# Seed script credentials (used by `npm run db:seed`)
SEED_OWNER_EMAIL="owner@boulderparc.com"
SEED_OWNER_PASSWORD="change-me-owner"
SEED_EDITOR_EMAIL="editor@boulderparc.com"
SEED_EDITOR_PASSWORD="change-me-editor"

# Figma integration (added in the template-import milestone)
FIGMA_ACCESS_TOKEN=""

# Object storage for uploaded assets/fonts/exports (added in the export milestone)
BLOB_READ_WRITE_TOKEN=""
